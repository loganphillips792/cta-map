services:
  cta-map:
    build:
      context: .
    ports:
      - "8080:8080"
    env_file:
      - compose.env
    environment:
      - PORT=8080
      - JAWG_ACCESS_TOKEN=${JAWG_ACCESS_TOKEN}
    volumes:
      # Mount data directory from host (read-write for API tracker db)
      - ./backend/data:/app/data
